package { 		import flash.display.*;	import flash.media.Sound;	public class Question extends flash.display.MovieClip { 		var text; // The main text of the question.		//Sound questionSound; // The sound to be played when the question is asked		var answers:Array = new Array(); // Answers One, two, and Three		var answerSounds:Array = new Array();  // The sounds to be played when the answer is selected		var qid; // This question's unique identifier in the database;		var jpg; // This question's unique identifier in the database;		var points; // This question's point worth		var  category; // This question's category... integer?			public function Question() {					}	}}